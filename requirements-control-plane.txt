# starlette==1.3.1: CVE-2026-48710 (BadHost). Must stay aligned with
# vendor/hermes-agent web extra + Dockerfile pin; the old >=0.38,<1 range
# resolved to vulnerable 0.52.x and blocked the 1.3.1 pin.
starlette==1.3.1
uvicorn>=0.30,<1
jinja2>=3.1,<4
httpx>=0.27,<1
PyYAML>=6,<7
