# v010 ready branch summary

This worktree is the integration gate for Hermes Vault v0.10.0. The branch now carries the release-facing documentation updates for unattended OAuth refresh, generic custom verifiers, and the new release notes, plus version bumps on the allowed version surfaces.

Validation is complete and summarized in `release-readiness/v0.10.0/readiness-report.md`.
